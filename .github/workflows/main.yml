name: CI

on: [push, workflow_dispatch]

jobs:
  build:

    runs-on: ubuntu-latest  # Menggunakan Ubuntu sebagai base image

    steps:
    - name: Download ngrok
      run: wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-stable-linux-amd64.zip -O ngrok.zip
    - name: Extract ngrok
      run: unzip ngrok.zip -d ngrok  # Mengextract ke direktori ngrok/
    - name: Install dependencies
      run: sudo apt-get update && sudo apt-get install -y openssh-server wget unzip
    - name: Auth ngrok
      run: ./ngrok/ngrok authtoken $NGROK_AUTH_TOKEN  # Menggunakan path ngrok yang sesuai setelah ekstraksi
      env:
        NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}
    - name: Enable SSH
      run: sudo systemctl enable ssh && sudo systemctl start ssh
    - name: Set user password
      run: |
        sudo useradd -m user  # Buat user baru
        echo 'user:root' | sudo chpasswd  # Atur password user
    - name: Create tunnel
      run: ./ngrok/ngrok tcp 22  # Mengarahkan port 22 (SSH) melalui ngrok
